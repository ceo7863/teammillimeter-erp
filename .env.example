VITE_USE_API=true
VITE_API_BASE=/api

# Server (.env in project root — read by server/index.mjs via process.env)
PORT=8080
JWT_SECRET=change-this-secret-before-production
DATABASE_PATH=./data/erp.sqlite
PDF_ARCHIVE_DIR=./data/pdf-archives
